
## targets

default: ProgramAlgebra
images: 
test: ProgramAlgebra

all: images test


## global settings

SRC = $(ISABELLE_HOME)/src
OUT = $(ISABELLE_OUTPUT)
LOG = $(OUT)/log

USEDIR = $(ISABELLE_TOOL) usedir -v true -i true -d pdf  ## -D generated


## ProgramAlgebra

ProgramAlgebra: $(LOG)/HOL-ProgramAlgebra.gz

$(LOG)/HOL-ProgramAlgebra.gz: ROOT.ML document/root.tex *.thy
	@$(USEDIR) HOL .


## clean

clean:
	@rm -f $(LOG)/HOL-ProgramAlgebra.gz
