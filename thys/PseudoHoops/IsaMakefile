## targets

default: PseudoHoops
images: 
test: PseudoHoops

all: images test


## global settings

SRC = $(ISABELLE_HOME)/src
OUT = $(ISABELLE_OUTPUT)
LOG = $(OUT)/log

USEDIR = $(ISABELLE_TOOL) usedir -v true -i true -d pdf

## PseudoHoops

PseudoHoops: $(LOG)/HOL-PseudoHoops.gz

$(LOG)/HOL-PseudoHoops.gz: ROOT.ML document/root.tex *.thy
	@$(USEDIR) HOL .



## clean

clean:
	@rm -f $(LOG)/HOL-PseudoHoops.gz
