pdf:
	./fixdoc
	$(ISABELLE_TOOL) latex -o sty
	$(ISABELLE_TOOL) latex -o pdf
	$(ISABELLE_TOOL) latex -o pdf

dvi:
	./fixdoc
	$(ISABELLE_TOOL) latex -o sty
	$(ISABELLE_TOOL) latex -o dvi
	$(ISABELLE_TOOL) latex -o dvi

